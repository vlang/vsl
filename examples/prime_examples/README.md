# prime_examples

Demonstrates the `vsl.prime` module:

- primality checks with `is_prime`
- generating prime numbers with `prime_sieve`

## Run

```sh
v run examples/prime_examples/main.v
```

## Expected behavior

- Prints `true/false` for a small set of numbers.
- Prints all primes smaller than `49`.

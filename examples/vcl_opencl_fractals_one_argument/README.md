# Creating fractals with VCL

This example shows how to create fractals with VCL using OpenCL as backend.

## Quickstart

```bash
v run main.v
```

after running it you should see the generated fractals in the directory `output/`.

#!/usr/bin/env bash

for module in roots diff deriv
do
    mkdir vsl/$module/vsl
    cp -r vsl/vsl.v vsl/fn.v vsl/internal vsl/$module/vsl
done

v -g test vsl

for module in roots diff deriv
do
    rm -rf vsl/$module/vsl
done

find . -name '*_test' | xargs rm -f 
